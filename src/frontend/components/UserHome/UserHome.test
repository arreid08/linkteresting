import React from 'react'
import { shallow, mount } from 'enzyme'
import UserHome from './index'



describe('User Home Here', () => {
  it("should have a div with className 'col-md-6'", () => {
    const component = mount(<UserHome />)
    expect(component.contains(<div class-name="col-md-6"></div>)).toBe(true)
  })
})
